name: abi pipeline

on: push

jobs:
  build-job:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Check Python
        run: python --version
